# SonarCloud configuration intentionally disabled (using Snyk instead)
# Leaving this file commented to avoid accidental scans.
# If you want to re-enable SonarCloud, uncomment and set values.
# sonar.organization=<your-org>
# sonar.projectKey=<your-org>_EV1-repo
# sonar.projectName=EV1-repo
# sonar.sources=.
# sonar.exclusions=**/node_modules/**,**/__tests__/**,**/*.spec.js