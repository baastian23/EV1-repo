{
  "name": "ev1-repo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "jest --ci"
  },
  "devDependencies": {
    "jest": "^30.2.0",
    "jest-environment-jsdom": "^30.2.0",
    "jsdom": "^27.4.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": [
      "**/__tests__/**/*.spec.js"
    ],
    "setupFiles": [
      "<rootDir>/jest.setup.js"
    ]
  }
}