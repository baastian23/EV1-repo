{
  "name": "ev1-repo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "jest --ci"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": [
      "**/__tests__/**/*.spec.js"
    ]
  }
}