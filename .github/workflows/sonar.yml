name: sonar (disabled)
on:
  # Deshabilitado: mantener archivo para referencia pero sin disparadores automáticos
  workflow_dispatch:
    inputs:
      note:
        description: "Este workflow está deshabilitado por decisión del proyecto (se usa Snyk)."
        required: false
jobs:
  sonarcloud:
    if: false  # bloqueado explícitamente
    runs-on: ubuntu-latest
    steps:
      - run: echo "SonarCloud deshabilitado; usar Snyk para SCA/SAST/Container."